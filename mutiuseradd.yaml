---
- name: adding new users list
  hosts: all
  become: true
  vars:
    ansible_user: gbiswas
    ansible_ssh_pass: Celerio@1000
    ansible_ssh_common_args: "-o StrictHostKeyChecking=no"
    name:
         - joe
         - gbiswas
         - mike
  tasks:
       - name: adding new users to servers
         user:
             name: "{{item}}"
             shell: /bin/bash
             create_home: true
         loop: "{{name}}"
       
